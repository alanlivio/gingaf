name: ccws
description: Ginga CC Web Services server/routing library
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  http: ^1.1.0
  logging: ^1.3.0

dev_dependencies:
  test: ^1.29.0
