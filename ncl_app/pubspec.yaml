name: ncl_app
description: Flutter package for Ginga NCL visual player app and widgets
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.3.0
  video_player: ^2.8.6
  ncl_doc:
    path: ../ncl_doc
  video_player_platform_interface: ^6.7.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  plugin_platform_interface: ^2.1.8
