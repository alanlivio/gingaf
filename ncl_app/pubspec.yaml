name: ncl_app
description: Flutter package for Ginga NCL visual player app and widgets
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  ncl_vm:
    path: ../ncl_vm

dev_dependencies:
  flutter_test:
    sdk: flutter
